node_modules
dist
.void
.wrangler
*.tsbuildinfo
.DS_Store
.env
.env.*
!.env.example

# generated
surface/dist
# synced by `bun run panel:sync` (public/_) and `bun run app:sync` (everything else)
public/*
.void-fresh
.void-mail
# Void projects generated by `voidbase deploy` for consumers
.cloud
# written by scripts/build-exe.ts while compiling the executables
src/node/embedded.generated.json
# rendered by scripts/ci-status.ts, deployed by Workers Builds as the status Worker (ci/wrangler.jsonc)
ci/public
# scratch app the adapter test converts
test/.tmp
