{
  "name": "voidbase-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Workspace root of the voidbase repository: the published package lives in packages/voidbase",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "bun": ">=1.2"
  },
  "scripts": {
    "build": "bun scripts/pipeline.ts build",
    "deploy": "bun scripts/pipeline.ts deploy",
    "version": "bun scripts/pipeline.ts version",
    "ci": "bash scripts/ci.sh",
    "release": "bash scripts/release.sh",
    "cf:builds": "bun scripts/cf-builds.ts",
    "build:exe": "bun scripts/build-exe.ts",
    "surface": "bun surface/render.ts",
    "check": "cd packages/voidbase && bun run check && cd ../plugin-backups && bun run check && cd ../plugin-domains && bun run check && cd ../plugin-hardening && bun run check && cd ../plugin-mail && bun run check && cd ../plugin-openapi && bun run check && cd ../plugin-observability && bun run check && cd ../plugin-previews && bun run check && cd ../plugin-realtime && bun run check && cd ../plugin-shipping-flat && bun run check && cd ../plugin-tax-flat && bun run check && cd ../plugin-translations && bun run check && cd ../.. && tsc -p tsconfig.scripts.json --noEmit",
    "test": "bun test",
    "dev": "cd packages/voidbase && bun run dev",
    "preview": "cd packages/voidbase && bun run preview",
    "build:app": "cd packages/voidbase && bun run build",
    "panel:sync": "cd packages/voidbase && bun run panel:sync",
    "app:sync": "cd packages/voidbase && bun run app:sync",
    "prepare": "husky || true"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250903.0",
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "@types/bun": "^1.4.1",
    "fflate": "^0.8.3",
    "husky": "^9.1.7",
    "typescript": "^5.9.3",
    "vite": "^8.0.10",
    "vite-plus": "^0.1.21",
    "void": "^0.10.13"
  }
}
